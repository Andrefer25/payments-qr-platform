{
  "name": "payments-qr-platform",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix apps/backend run build && npm --prefix apps/frontend run build",
    "typecheck": "npm --prefix apps/backend run typecheck && npm --prefix apps/frontend run typecheck",
    "test": "npm --prefix apps/backend test && npm --prefix apps/frontend test",
    "audit": "npm --prefix apps/backend audit && npm --prefix apps/frontend audit",
    "infra:init": "npm --prefix apps/backend run infra:init",
    "compose:up": "docker compose up --build",
    "compose:dev": "docker compose -f docker-compose.yml -f docker-compose.dev.yml up --build",
    "compose:down": "docker compose down"
  }
}
